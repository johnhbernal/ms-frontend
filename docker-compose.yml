services:
  ms-frontend:
    build:
      context: .
      args:
        REACT_APP_AUTH_API_URL: ${REACT_APP_AUTH_API_URL:-http://localhost:8081}
        REACT_APP_PRACTICA_API_URL: ${REACT_APP_PRACTICA_API_URL:-http://localhost:8082/api}
    ports:
      - "3000:8080"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
